build the aar by using the command line
go to this directory (contains this readMe_buld.txt file AND the gradlew.bat)
    gradlew clean build
The aars will be created in
../xxxxxxx/build/outputs/aar

Then add the xxxx-release.aar to the lib directory of the project you want to build
